version: 2.1

jobs:
  backend_ci:
    docker:
      - image: cimg/python:3.11
    steps:
      - checkout
      - run:
          name: Install Backend Dependencies
          command: |
            cd backend
            if [ -f requirements.txt ]; then
              pip install -r requirements.txt
            else
              echo "No requirements.txt found"
            fi
      - run:
          name: Run Backend Tests
          command: |
            cd backend
            pytest || echo "No tests"

  frontend_ci:
    docker:
      - image: cimg/node:18.0
    steps:
      - checkout
      - run:
          name: Install Frontend Dependencies
          command: |
            cd frontend
            if [ -f package.json ]; then
              npm install
              npm run build
            else
              echo "No package.json found"
            fi

  deploy_to_ec2:
    docker:
      - image: cimg/base:stable
    steps:
      - add_ssh_keys:
          fingerprints:
            - "SHA256:5P6JK74PEOzVt3dco21PcW7MFIU47fGI7fn6YlbDV9g"
      - run:
          name: Deploy
          command: |
            ssh -o StrictHostKeyChecking=no ec2-user@$100.53.189.58 "
              if [ ! -d /home/ec2-user/blog-app ]; then
                git clone https://github.com/Sudhirgithib/circle-clone.git /home/ec2-user/blog-app
              fi &&
              cd /home/ec2-user/blog-app && git pull origin main &&
              npm install --prefix fron
            

workflows:
  ci_cd_pipeline:
    jobs:
      - backend_ci
      - frontend_ci
      - deploy_to_ec2:
          requires:
            - backend_ci
            - frontend_ci